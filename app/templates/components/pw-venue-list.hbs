<ul class="nav nav-sidebar">
  {{#each venues as |venue|}}
    <li>
      <a href="#">
        <img src="{{venue.avatar}}" class="venue__avatar" />
        {{venue.name}}
      </a>
    </li>
  {{else}}
    <li><a href="#">No venues.</a></li>
  {{/each}}
</ul>
